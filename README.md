# RPI_installer_components
Node-Red, Influxdb, grafana installation

$ sudo apt update
$ sudo apt upgrade -y


##	Installation procedure

      $ sudo apt update
      $ sudo apt upgrade -y

## Run script
      $ sudo ./install.sh
